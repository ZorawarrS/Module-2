import numpy as np 
import matplotlib.pyplot as plt


# xpoints = [1,3]
# ypoints = [1,3]

# x1, x2 = 1,3
# y1, y2 = 1,3

# Z = np.array([[1, x1], [1, x2]])

# Y = np.array([y1, y2])

# A = np.linalg.solve(Z, Y)

# print("A = ", A)

# a1, a2 = A
# xValue = 2
# yValue = a1 +a2*xValue
# print("At x = 2, y = ", yValue)

# x = np.linspace(0,5,100)
# y = a1 + a2*x
# plt.plot(x,y)

# plt.scatter(xpoints, ypoints, color = 'blue', s=60, label = 'Known Points')

# plt.scatter(xValue, yValue, color = 'red', zorder = 5, label = f'Interpolated Point at x = {xValue}, y = {yValue:.2f}')

# plt.title ('Line with Interpolated Point (red)')
# plt.xlabel('x')
# plt.ylabel('y')
# plt.grid(True)
# plt.legend()
# plt.show()

xpoints = [1, 3, 5]
ypoints = [1, 3, 2]

x1, x2, x3, = 1, 3, 5
y1, y2, y3 = 1, 3, 2

Z = np.array([[1, x1, x1**2], [1, x2, x2**2], [1, x3, x3**2]])

Y = np.array([y1, y2, y3])

A = np.linalg.solve(Z, Y)
print("A = ", A)

a1, a2, a3 = A